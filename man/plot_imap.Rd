% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-visualize.R
\name{plot_imap}
\alias{plot_imap}
\title{Plot a map for inverse distance weighting (IDW) interpolation}
\usage{
plot_imap(
  dat,
  fill = colorRampPalette(RColorBrewer::brewer.pal(11, "RdYlGn"))(100) \%>\% rev,
  color = "gray50",
  alpha = 0.8,
  width = 0.8,
  size = 12,
  gg.theme = theme_bw(),
  bg.color = "white",
  gd.color = "gray80",
  legend.position = c("right", "left", "top", "bottom"),
  legend.title.size = 14,
  legend.text.size = 12,
  show.legend.rect = FALSE
)
}
\arguments{
\item{dat}{An R list returned by \code{microgeo::interp_idw()}.}

\item{fill}{Fill color of map area. Default is `rev(colorRampPalette(RColorBrewer::brewer.pal(11, 'RdYlGn'))(100))`.}

\item{color}{Line color of map border. Default is `gray50`.}

\item{alpha}{Transparency of fill color. Default is `0.8`.}

\item{width}{Line width of map border. Default is `0.8`.}

\item{size}{Size of axis text. Default is `12`.}

\item{gg.theme}{The ggplot2 theme. Default is `theme_bw()`.}

\item{bg.color}{Color of panel background. Default is `white`.}

\item{gd.color}{Color of panel grid major. Default is `gray80`.}

\item{legend.position}{Legend position. Select a value from `'right', 'left', 'top', 'bottom'` or set a numeric vector
like `c(0.1, 0.2)`. Default is `right`.}

\item{legend.title.size}{Text size of legend title. Default is `14`.}

\item{legend.text.size}{Text size of legend label. Default is `12`.}

\item{show.legend.rect}{Should the rectangle around the legend be displayed? Default is `FALSE`.}
}
\value{
A ggplot2 object.
}
\description{
This function is used to visualize the results of inverse distance weighting (IDW) interpolation. It only
works when `<type> is 'hexagonal'` in running \code{microgeo::interp_idw()}.
}
\examples{
data(idw.interp.hex)
idw.interp.hex$shannon$type # should be "idw_hex"
idw.interp.hex$shannon \%>\% plot_imap()
idw.interp.hex$observed \%>\% plot_imap()
idw.interp.hex$observed \%>\% plot_imap(show.legend.rect = TRUE)
idw.interp.hex$observed \%>\% plot_imap(fill = rev(colorRampPalette(RColorBrewer::brewer.pal(11, 'Spectral'))(100)))
}
\seealso{
\code{\link[microgeo:interp_idw]{microgeo::interp_idw()}}
}
\author{
Li Chaonan (Ecological Security and Protection Key Laboratory of Sichuan Province, Mianyang Normal University)
}
